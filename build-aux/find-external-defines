#!/bin/sh
# Find #defines that are exported from the main library

# Get internal headers that correspond to public headers
HEADERS=`grep "^internal_HDRS = " Makefile | cut -d " " -f 3-`

# Find #defined symbols
grep "^#define" $HEADERS | cut -d " " -f 2 | cut -d "(" -f 1
